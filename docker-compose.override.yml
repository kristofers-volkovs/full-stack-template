services:
  backend:
    restart: "no"
    build:
      context: .
      dockerfile: backend/Dockerfile
      target: dev
    command: bash scripts/startup.sh --dev
    volumes:
      - ./backend/src/:/app/src/
      - ./backend/tests/:/app/tests/

  db:
    restart: "no"
